// TypeScript entrypoint note:
// The runnable implementation is scripts/capture_showcase_screenshots.mjs so it
// can execute directly with Node in the delivered environment. It uses
// Playwright and sharp from web/studio devDependencies.
