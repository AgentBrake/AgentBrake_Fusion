# Frontend Wrapper

The production frontend source is in `web/studio`. This directory exists to satisfy submission layouts that expect a top-level `frontend/` project.

Common commands:

```bash
npm run install:studio
npm run dev
npm run build
```
